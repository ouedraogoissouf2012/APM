/// Session modes — protocol values shared with the backend (`SessionStartIn.mode`).
const String kSessionModeFree = 'free';
const String kSessionModeScenario = 'scenario';
const String kSessionModeMission = 'mission';
